import { delegateIPv6Prefix } from "../prefix-delegation.js";
import { t } from "../../../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">${t("network")}</p><h1>${de ? "IPv6-Präfixdelegation" : "IPv6 Prefix Delegation"}</h1><p class="page-summary">${de ? "Ein IPv6-Präfix in kleinere Netze aufteilen." : "Split an IPv6 prefix into child networks."}</p></div></header><section class="card tool-workspace"><label>Address<input id="pd-address" value="2001:db8::"></label><label>Parent prefix<input id="pd-parent" type="number" min="0" max="128" value="62"></label><label>Child prefix<input id="pd-child" type="number" min="0" max="128" value="64"></label><button id="pd-generate" type="button">${de ? "Erzeugen" : "Generate"}</button><p id="pd-error" role="alert"></p><output id="pd-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const value = (id) => container.querySelector(id).value; const output = container.querySelector("#pd-output"); const error = container.querySelector("#pd-error"); container.querySelector("#pd-generate").addEventListener("click", () => { try { output.textContent = delegateIPv6Prefix(value("#pd-address"), value("#pd-parent"), value("#pd-child")).join("\n"); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }); }
