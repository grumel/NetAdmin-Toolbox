import { diagnoseTarget } from "../diagnostics.js";
import { t } from "../../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">${t("network")}</p><h1>${de ? "Netzwerkdiagnose" : "Network Diagnostics"}</h1><p class="page-summary">${de ? "IPv4-Adressen und Hostnamen lokal analysieren." : "Analyze IPv4 addresses and hostnames locally."}</p></div></header><section class="card tool-workspace"><label>Target<input id="diag-target" value="192.168.1.10" autocomplete="off"></label><button id="diag-run" type="button">${de ? "Analysieren" : "Analyze"}</button><p id="diag-error" role="alert"></p><output id="diag-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const input = container.querySelector("#diag-target"); const output = container.querySelector("#diag-output"); const error = container.querySelector("#diag-error"); container.querySelector("#diag-run").addEventListener("click", () => { try { const result = diagnoseTarget(input.value); output.textContent = `Kind: ${result.kind}; Klasse: ${result.addressClass}; Lokal: ${result.local ? "yes" : "no"}`; error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }); }
