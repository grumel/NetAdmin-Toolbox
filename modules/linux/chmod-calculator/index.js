import { chmodToSymbolic } from "../chmod-calculator.js";
import { t } from "../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Linux</p><h1>chmod Calculator</h1><p class="page-summary">${de ? "Unix-Berechtigungen lokal umwandeln." : "Convert Unix permissions locally."}</p></div></header><section class="card tool-workspace"><label>${de ? "Oktalmodus" : "Octal mode"}<input id="chmod-mode" inputmode="numeric" placeholder="755"></label><button id="chmod-calculate" type="button">${de ? "Berechnen" : "Calculate"}</button><p id="chmod-error" role="alert"></p><output id="chmod-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const input = container.querySelector("#chmod-mode"); const output = container.querySelector("#chmod-output"); const error = container.querySelector("#chmod-error"); const run = () => { try { output.textContent = chmodToSymbolic(input.value); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }; container.querySelector("#chmod-calculate").addEventListener("click", run); input.addEventListener("input", () => { if (input.value.length === 3) run(); }); }
