import { buildCron } from "../cron-generator.js";
import { t } from "../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Linux</p><h1>Cron Generator</h1><p class="page-summary">${de ? "Cron-Zeilen lokal erzeugen." : "Generate cron entries locally."}</p></div></header><section class="card tool-workspace"><label>Minute<input id="cron-minute" value="*"></label><label>Hour<input id="cron-hour" value="*"></label><label>Day<input id="cron-day" value="*"></label><label>Month<input id="cron-month" value="*"></label><label>Weekday<input id="cron-weekday" value="*"></label><label>${de ? "Befehl" : "Command"}<input id="cron-command"></label><button id="cron-build" type="button">${de ? "Erzeugen" : "Generate"}</button><p id="cron-error" role="alert"></p><output id="cron-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const error = container.querySelector("#cron-error"); const output = container.querySelector("#cron-output"); container.querySelector("#cron-build").addEventListener("click", () => { try { const get = (id) => container.querySelector(id).value; output.textContent = buildCron({ minute:get("#cron-minute"), hour:get("#cron-hour"), day:get("#cron-day"), month:get("#cron-month"), weekday:get("#cron-weekday"), command:get("#cron-command") }); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }); }
