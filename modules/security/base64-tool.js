export function encodeBase64(value) { const bytes = new TextEncoder().encode(String(value ?? "")); let binary = ""; for (const byte of bytes) binary += String.fromCharCode(byte); return btoa(binary); }
export function decodeBase64(value) { const text = String(value ?? "").trim(); if (!/^[A-Za-z0-9+/]*={0,2}$/.test(text) || text.length % 4 === 1) throw new TypeError("Enter valid Base64"); try { const binary = atob(text); return new TextDecoder("utf-8", { fatal: true }).decode(Uint8Array.from(binary, (char) => char.charCodeAt(0))); } catch { throw new TypeError("Enter valid Base64"); } }
