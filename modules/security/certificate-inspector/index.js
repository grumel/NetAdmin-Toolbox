import { inspectCertificate } from "../certificate-inspector.js";
import { t } from "../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Security</p><h1>${de ? "Zertifikatsinspektor" : "Certificate Inspector"}</h1><p class="page-summary">${de ? "PEM-Zertifikatshüllen lokal prüfen." : "Inspect PEM certificate envelopes locally."}</p></div></header><section class="card tool-workspace"><label>PEM certificate<textarea id="cert-pem" rows="8"></textarea></label><button id="cert-inspect" type="button">${de ? "Prüfen" : "Inspect"}</button><p id="cert-error" role="alert"></p><output id="cert-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const output = container.querySelector("#cert-output"); const error = container.querySelector("#cert-error"); container.querySelector("#cert-inspect").addEventListener("click", () => { try { const result = inspectCertificate(container.querySelector("#cert-pem").value); output.textContent = `Format: ${result.format}; Bytes: ${result.byteLength}; Private key: ${result.hasPrivateKey ? "yes" : "no"}`; error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }); }
