import { decodeJwt } from "../jwt-decoder.js";
import { t } from "../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Security</p><h1>JWT Decoder</h1><p class="page-summary">${de ? "JWT-Header und Payload lokal dekodieren (keine Signaturprüfung)." : "Decode JWT headers and payloads locally (no signature verification)."}</p></div></header><section class="card tool-workspace"><label>Token<textarea id="jwt-token" rows="6"></textarea></label><button id="jwt-decode" type="button">${de ? "Dekodieren" : "Decode"}</button><p id="jwt-error" role="alert"></p><output id="jwt-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const output = container.querySelector("#jwt-output"); const error = container.querySelector("#jwt-error"); container.querySelector("#jwt-decode").addEventListener("click", () => { try { const result = decodeJwt(container.querySelector("#jwt-token").value); output.textContent = `Header: ${JSON.stringify(result.header)}\nPayload: ${JSON.stringify(result.payload)}`; error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }); }
