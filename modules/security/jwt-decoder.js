function decodePart(value) { const normalized = String(value).replace(/-/g, "+").replace(/_/g, "/"); const padded = normalized.padEnd(Math.ceil(normalized.length / 4) * 4, "="); try { return JSON.parse(new TextDecoder().decode(Uint8Array.from(atob(padded), (char) => char.charCodeAt(0)))); } catch { throw new TypeError("JWT contains invalid JSON"); } }
export function decodeJwt(token) { const parts = String(token ?? "").trim().split("."); if (parts.length !== 3) throw new TypeError("JWT must contain three segments"); return { header: decodePart(parts[0]), payload: decodePart(parts[1]), signature: parts[2] }; }
