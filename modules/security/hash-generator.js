export async function hashText(value, algorithm = "SHA-256") { const allowed = ["SHA-256", "SHA-384", "SHA-512"]; if (!allowed.includes(algorithm)) throw new TypeError("Unsupported hash algorithm"); if (!globalThis.crypto?.subtle) throw new Error("Web Crypto is unavailable"); const data = new TextEncoder().encode(String(value ?? "")); const digest = await globalThis.crypto.subtle.digest(algorithm, data); return [...new Uint8Array(digest)].map((byte) => byte.toString(16).padStart(2, "0")).join(""); }
