import { hashText } from "../hash-generator.js";
import { t } from "../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Security</p><h1>${de ? "Hashgenerator" : "Hash Generator"}</h1><p class="page-summary">${de ? "Text lokal mit SHA-Algorithmen hashen." : "Hash text locally with SHA algorithms."}</p></div></header><section class="card tool-workspace"><label>Text<textarea id="hash-text" rows="5"></textarea></label><label>Algorithm<select id="hash-algorithm"><option>SHA-256</option><option>SHA-384</option><option>SHA-512</option></select></label><button id="hash-generate" type="button">${de ? "Hash erzeugen" : "Generate hash"}</button><p id="hash-error" role="alert"></p><output id="hash-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const output = container.querySelector("#hash-output"); const error = container.querySelector("#hash-error"); container.querySelector("#hash-generate").addEventListener("click", async () => { try { output.textContent = await hashText(container.querySelector("#hash-text").value, container.querySelector("#hash-algorithm").value); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }); }
