import { generatePassword } from "../password-generator.js";
import { t } from "../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Security</p><h1>${de ? "Passwortgenerator" : "Password Generator"}</h1><p class="page-summary">${de ? "Zufällige Passwörter lokal erzeugen." : "Generate random passwords locally."}</p></div></header><section class="card tool-workspace"><label>Length<input id="pw-length" type="number" min="8" max="128" value="20"></label><button id="pw-generate" type="button">${de ? "Erzeugen" : "Generate"}</button><p id="pw-error" role="alert"></p><output id="pw-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const output = container.querySelector("#pw-output"); const error = container.querySelector("#pw-error"); container.querySelector("#pw-generate").addEventListener("click", () => { try { output.textContent = generatePassword(container.querySelector("#pw-length").value); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }); }
