import { decodeBase64, encodeBase64 } from "../base64-tool.js";
import { t } from "../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Security</p><h1>Base64 Tool</h1><p class="page-summary">${de ? "UTF-8-Text lokal mit Base64 kodieren und dekodieren." : "Encode and decode UTF-8 text with Base64 locally."}</p></div></header><section class="card tool-workspace"><label>Input<textarea id="b64-input" rows="6"></textarea></label><button id="b64-encode" type="button">Encode</button><button id="b64-decode" type="button">Decode</button><p id="b64-error" role="alert"></p><output id="b64-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const input = container.querySelector("#b64-input"); const output = container.querySelector("#b64-output"); const error = container.querySelector("#b64-error"); const run = (fn) => { try { output.textContent = fn(input.value); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }; container.querySelector("#b64-encode").addEventListener("click", () => run(encodeBase64)); container.querySelector("#b64-decode").addEventListener("click", () => run(decodeBase64)); }
