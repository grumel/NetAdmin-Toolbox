import { buildDhcpScope } from "../dhcp-helper.js";
import { t } from "../../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Windows</p><h1>DHCP Helper</h1><p class="page-summary">${de ? "PowerShell-Befehle für DHCPv4-Bereiche erzeugen." : "Generate PowerShell commands for DHCPv4 scopes."}</p></div></header><section class="card tool-workspace"><form id="dhcp-form" class="tool-form"><label>Name<input name="name" value="Office"></label><label>Network<input name="network" value="192.0.2.0"></label><label>Prefix<input name="prefix" type="number" min="0" max="32" value="24"></label><label>Start<input name="start" value="192.0.2.10"></label><label>End<input name="end" value="192.0.2.200"></label><button type="submit">${de ? "Erzeugen" : "Generate"}</button></form><p id="dhcp-error" role="alert"></p><output id="dhcp-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const form = container.querySelector("#dhcp-form"); const output = container.querySelector("#dhcp-output"); const error = container.querySelector("#dhcp-error"); form.addEventListener("submit", (event) => { event.preventDefault(); try { output.textContent = buildDhcpScope(Object.fromEntries(new FormData(form))).command; error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }); }
