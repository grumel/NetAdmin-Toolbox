import { generateGuid, parseSid } from "../sid-guid-helper.js";
import { t } from "../../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Windows</p><h1>SID / GUID Helper</h1><p class="page-summary">${de ? "Windows-SIDs analysieren und GUIDs erzeugen." : "Inspect Windows SIDs and generate GUIDs."}</p></div></header><section class="card tool-workspace"><label>SID<input id="sid-input" value="S-1-5-21-100-200-300-1001"></label><button id="sid-parse" type="button">${de ? "Analysieren" : "Parse SID"}</button><button id="guid-generate" type="button">${de ? "GUID erzeugen" : "Generate GUID"}</button><p id="sid-error" role="alert"></p><output id="sid-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const input = container.querySelector("#sid-input"); const output = container.querySelector("#sid-output"); const error = container.querySelector("#sid-error"); container.querySelector("#sid-parse").addEventListener("click", () => { try { const result = parseSid(input.value); output.textContent = `Revision: ${result.revision}; Authority: ${result.identifierAuthority}; Sub-authorities: ${result.subAuthorities.join(", ")}`; error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }); container.querySelector("#guid-generate").addEventListener("click", () => { output.textContent = generateGuid(); error.textContent = ""; }); }
