import { buildRegistryGet, buildRegistrySet } from "../registry-helper.js";
import { t } from "../../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Windows</p><h1>Registry Helper</h1><p class="page-summary">${de ? "Sichere PowerShell-Registry-Befehle erzeugen." : "Generate safe PowerShell registry commands."}</p></div></header><section class="card tool-workspace"><label>Path<input id="reg-path" value="HKLM:\\Software\\NetAdmin"></label><label>Name<input id="reg-name" value="Enabled"></label><label>Value<input id="reg-value" value="1"></label><button id="reg-get" type="button">${de ? "Lesen" : "Read"}</button><button id="reg-set" type="button">${de ? "Setzen" : "Set"}</button><p id="reg-error" role="alert"></p><output id="reg-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const path = container.querySelector("#reg-path"); const name = container.querySelector("#reg-name"); const value = container.querySelector("#reg-value"); const output = container.querySelector("#reg-output"); const error = container.querySelector("#reg-error"); const run = (fn) => { try { output.textContent = fn(); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }; container.querySelector("#reg-get").addEventListener("click", () => run(() => buildRegistryGet({ path: path.value, name: name.value }))); container.querySelector("#reg-set").addEventListener("click", () => run(() => buildRegistrySet({ path: path.value, name: name.value, value: value.value }))); }
