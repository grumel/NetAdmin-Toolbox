import { generateDnsCommand, generatePingCommand } from "../powershell-generator.js";
import { t } from "../../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Windows</p><h1>PowerShell Generator</h1><p class="page-summary">${de ? "Sichere PowerShell-Befehle für Ping und DNS erzeugen." : "Generate safe PowerShell commands for ping and DNS."}</p></div></header><section class="card tool-workspace"><label>Host<input id="ps-host" value="example.com"></label><label>Count<input id="ps-count" type="number" min="1" max="100" value="4"></label><button id="ps-ping" type="button">Ping</button><button id="ps-dns" type="button">DNS</button><p id="ps-error" role="alert"></p><pre id="ps-output" aria-live="polite"></pre></section></section>`; }
export function initialize(container) { const host = container.querySelector("#ps-host"); const count = container.querySelector("#ps-count"); const output = container.querySelector("#ps-output"); const error = container.querySelector("#ps-error"); const run = (fn) => { try { output.textContent = fn(); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }; container.querySelector("#ps-ping").addEventListener("click", () => run(() => generatePingCommand(host.value, count.value))); container.querySelector("#ps-dns").addEventListener("click", () => run(() => generateDnsCommand(host.value))); }
