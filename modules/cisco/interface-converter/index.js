import { convertInterface } from "../interface-converter.js";
import { t } from "../../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Cisco</p><h1>${de ? "Interface-Konverter" : "Interface Converter"}</h1><p class="page-summary">${de ? "Cisco-Interface-Kurzformen und vollständige Namen umwandeln." : "Convert Cisco interface abbreviations and canonical names."}</p></div></header><section class="card tool-workspace"><label for="interface-input">${de ? "Interface" : "Interface"}</label><input id="interface-input" value="Gi1/0/24" autocomplete="off" spellcheck="false"><button id="interface-convert" type="button">${de ? "Umwandeln" : "Convert"}</button><p id="interface-error" role="alert"></p><dl id="interface-output" class="result-list" aria-live="polite"></dl></section></section>`; }
export function initialize(container) { const input = container.querySelector("#interface-input"); const output = container.querySelector("#interface-output"); const error = container.querySelector("#interface-error"); container.querySelector("#interface-convert").addEventListener("click", () => { try { const result = convertInterface(input.value); output.innerHTML = `<div class="result-row"><dt>Canonical</dt><dd>${result.canonical}</dd></div><div class="result-row"><dt>Abbreviated</dt><dd>${result.abbreviated}</dd></div><div class="result-row"><dt>Type</dt><dd>${result.type}</dd></div>`; error.textContent = ""; } catch (exception) { output.innerHTML = ""; error.textContent = exception.message; } }); }
