import { buildVlanConfig } from "../vlan-calculator.js";
import { t } from "../../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Cisco</p><h1>${de ? "VLAN-Rechner" : "VLAN Calculator"}</h1><p class="page-summary">${de ? "VLAN-IDs prüfen und Cisco-Konfiguration erzeugen." : "Validate VLAN IDs and generate Cisco configuration."}</p></div></header><section class="card tool-workspace"><form id="vlan-form" class="tool-form"><label>${de ? "VLAN-ID" : "VLAN ID"}<input name="id" type="number" min="1" max="4094" value="10" required></label><label>Name<input name="name" value="Users" maxlength="32" required></label><label>${de ? "Schnittstelle (optional)" : "Interface (optional)"}<input name="interfaceName" placeholder="GigabitEthernet1/0/1"></label><label>${de ? "Modus" : "Mode"}<select name="mode"><option value="access">access</option><option value="trunk">trunk</option></select></label><button type="submit">${de ? "Konfiguration erzeugen" : "Generate configuration"}</button></form><p id="vlan-error" role="alert"></p><pre id="vlan-output" aria-live="polite"></pre></section></section>`; }
export function initialize(container) { const form = container.querySelector("#vlan-form"); const output = container.querySelector("#vlan-output"); const error = container.querySelector("#vlan-error"); const run = () => { try { output.textContent = buildVlanConfig(Object.fromEntries(new FormData(form))); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }; form.addEventListener("submit", (event) => { event.preventDefault(); run(); }); run(); }
