import { buildMplsConfig } from "../mpls-helper.js";
import { t } from "../../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Cisco</p><h1>MPLS Helper</h1><p class="page-summary">${de ? "Cisco-MPLS-Label-Konfiguration erzeugen." : "Generate Cisco MPLS label configuration."}</p></div></header><section class="card tool-workspace"><label>Router ID<input id="mpls-id" value="10.0.0.1"></label><label>Interfaces (comma separated)<input id="mpls-ifaces" value="GigabitEthernet1/0/1"></label><button id="mpls-generate" type="button">${de ? "Erzeugen" : "Generate"}</button><p id="mpls-error" role="alert"></p><output id="mpls-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const output = container.querySelector("#mpls-output"); const error = container.querySelector("#mpls-error"); container.querySelector("#mpls-generate").addEventListener("click", () => { try { output.textContent = buildMplsConfig({ routerId: container.querySelector("#mpls-id").value, interfaces: container.querySelector("#mpls-ifaces").value.split(",") }); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }); }
