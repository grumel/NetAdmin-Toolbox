import { buildStpConfig } from "../stp-helper.js";
import { t } from "../../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Cisco</p><h1>STP Helper</h1><p class="page-summary">${de ? "Cisco-Spanning-Tree-Konfiguration erzeugen." : "Generate Cisco Spanning Tree configuration."}</p></div></header><section class="card tool-workspace"><label>VLAN<input id="stp-vlan" type="number" min="1" max="4094" value="10"></label><label>Role<select id="stp-role"><option value="root primary">root primary</option><option value="root secondary">root secondary</option><option value="none">none</option></select></label><label>Priority<input id="stp-priority" type="number" placeholder="4096"></label><button id="stp-generate" type="button">${de ? "Erzeugen" : "Generate"}</button><p id="stp-error" role="alert"></p><output id="stp-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const vlan = container.querySelector("#stp-vlan"); const role = container.querySelector("#stp-role"); const priority = container.querySelector("#stp-priority"); const output = container.querySelector("#stp-output"); const error = container.querySelector("#stp-error"); container.querySelector("#stp-generate").addEventListener("click", () => { try { output.textContent = buildStpConfig({ vlan: vlan.value, role: role.value, priority: priority.value }); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }); }
