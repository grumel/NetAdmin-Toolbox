import { buildQosPolicy } from "../qos-helper.js";
import { t } from "../../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Cisco</p><h1>QoS Helper</h1><p class="page-summary">${de ? "Cisco-QoS-Klassifizierung und Priorisierung erzeugen." : "Generate Cisco QoS classification and priority configuration."}</p></div></header><section class="card tool-workspace"><label>Policy name<input id="qos-name" value="EDGE-QOS"></label><label>Class name<input id="qos-class" value="VOICE"></label><label>DSCP<input id="qos-dscp" value="EF"></label><label>Priority Mbps<input id="qos-rate" type="number" min="1" max="100" value="10"></label><button id="qos-generate" type="button">${de ? "Erzeugen" : "Generate"}</button><p id="qos-error" role="alert"></p><output id="qos-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const get = (id) => container.querySelector(id).value; const output = container.querySelector("#qos-output"); const error = container.querySelector("#qos-error"); container.querySelector("#qos-generate").addEventListener("click", () => { try { output.textContent = buildQosPolicy({ name: get("#qos-name"), className: get("#qos-class"), dscp: get("#qos-dscp"), bandwidth: get("#qos-rate") }); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }); }
