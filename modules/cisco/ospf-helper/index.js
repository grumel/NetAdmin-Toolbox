import { buildOspfNetwork } from "../ospf-helper.js";
import { t } from "../../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Cisco</p><h1>OSPF Helper</h1><p class="page-summary">${de ? "OSPF-Netzwerkbefehle mit Wildcard-Maske erzeugen." : "Generate OSPF network statements with wildcard masks."}</p></div></header><section class="card tool-workspace"><form id="ospf-form" class="tool-form"><label>Network<input name="network" value="10.0.0.0"></label><label>Prefix<input name="prefix" type="number" min="0" max="32" value="24"></label><label>Area<input name="area" value="0"></label><button type="submit">${de ? "Erzeugen" : "Generate"}</button></form><p id="ospf-error" role="alert"></p><output id="ospf-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const form = container.querySelector("#ospf-form"); const output = container.querySelector("#ospf-output"); const error = container.querySelector("#ospf-error"); form.addEventListener("submit", (event) => { event.preventDefault(); try { output.textContent = buildOspfNetwork(Object.fromEntries(new FormData(form))).command.trim(); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }); }
