import { buildVxlanConfig } from "../vxlan-helper.js";
import { t } from "../../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Cisco</p><h1>VXLAN Helper</h1><p class="page-summary">${de ? "Cisco-NVE-Konfigurationen für VXLAN erzeugen." : "Generate Cisco NVE configuration for VXLAN."}</p></div></header><section class="card tool-workspace"><label>VNI<input id="vxlan-vni" type="number" min="1" max="16777215" value="10010"></label><label>VLAN<input id="vxlan-vlan" type="number" min="1" max="4094" value="10"></label><label>Source interface<input id="vxlan-source" value="Loopback0"></label><button id="vxlan-generate" type="button">${de ? "Erzeugen" : "Generate"}</button><p id="vxlan-error" role="alert"></p><output id="vxlan-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const value = (id) => container.querySelector(id).value; const output = container.querySelector("#vxlan-output"); const error = container.querySelector("#vxlan-error"); container.querySelector("#vxlan-generate").addEventListener("click", () => { try { output.textContent = buildVxlanConfig({ vni: value("#vxlan-vni"), vlan: value("#vxlan-vlan"), sourceInterface: value("#vxlan-source") }); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }); }
