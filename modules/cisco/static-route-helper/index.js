import { buildStaticRoute } from "../static-route-helper.js";
import { t } from "../../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">Cisco</p><h1>${de ? "Static-Route-Helfer" : "Static Route Helper"}</h1><p class="page-summary">${de ? "Statische Cisco-Routen mit Next-Hop und Distanz erzeugen." : "Generate Cisco static routes with next hop and distance."}</p></div></header><section class="card tool-workspace"><form id="route-form" class="tool-form"><label>Network<input name="network" value="192.0.2.0"></label><label>Prefix<input name="prefix" type="number" min="0" max="32" value="24"></label><label>Next hop<input name="nextHop" value="192.0.2.1"></label><label>${de ? "Administrative Distanz (optional)" : "Administrative distance (optional)"}<input name="distance" type="number" min="1" max="255" placeholder="1"></label><button type="submit">${de ? "Erzeugen" : "Generate"}</button></form><p id="route-error" role="alert"></p><output id="route-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const form = container.querySelector("#route-form"); const output = container.querySelector("#route-output"); const error = container.querySelector("#route-error"); form.addEventListener("submit", (event) => { event.preventDefault(); try { output.textContent = buildStaticRoute(Object.fromEntries(new FormData(form))).command; error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }); }
