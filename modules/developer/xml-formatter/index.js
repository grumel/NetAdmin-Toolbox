import { formatXml } from "../xml-formatter.js";
import { t } from "../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">${de ? "Entwicklung" : "Developer"}</p><h1>XML Formatter</h1><p class="page-summary">${de ? "XML lokal prüfen und lesbar formatieren." : "Validate and pretty-print XML locally."}</p></div></header><section class="card tool-workspace"><label>${de ? "XML-Eingabe" : "XML input"}<textarea id="xml-input" rows="10" spellcheck="false"></textarea></label><label>${de ? "Einrückung" : "Indentation"}<select id="xml-spaces"><option value="2">2</option><option value="4">4</option></select></label><button id="xml-format" type="button">${de ? "Formatieren" : "Format"}</button><p id="xml-error" role="alert"></p><output id="xml-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const input = container.querySelector("#xml-input"); const output = container.querySelector("#xml-output"); const error = container.querySelector("#xml-error"); const run = () => { try { output.textContent = formatXml(input.value, Number(container.querySelector("#xml-spaces").value)); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }; container.querySelector("#xml-format").addEventListener("click", run); input.addEventListener("input", () => { if (input.value.trim()) run(); }); }
