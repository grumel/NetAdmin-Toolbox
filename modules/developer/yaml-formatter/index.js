import { formatYaml } from "../yaml-formatter.js";
import { t } from "../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">${de ? "Entwicklung" : "Developer"}</p><h1>YAML Formatter</h1><p class="page-summary">${de ? "YAML lokal lesbar formatieren." : "Format YAML locally without dependencies."}</p></div></header><section class="card tool-workspace"><label>${de ? "YAML-Eingabe" : "YAML input"}<textarea id="yaml-input" rows="10" spellcheck="false"></textarea></label><label>${de ? "Einrückung" : "Indentation"}<select id="yaml-spaces"><option value="2">2</option><option value="4">4</option></select></label><button id="yaml-format" type="button">${de ? "Formatieren" : "Format"}</button><p id="yaml-error" role="alert"></p><output id="yaml-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const input = container.querySelector("#yaml-input"); const output = container.querySelector("#yaml-output"); const error = container.querySelector("#yaml-error"); const run = () => { try { output.textContent = formatYaml(input.value, Number(container.querySelector("#yaml-spaces").value)); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }; container.querySelector("#yaml-format").addEventListener("click", run); input.addEventListener("input", () => { if (input.value.trim()) run(); }); }
