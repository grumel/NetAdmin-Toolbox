import { testRegex } from "../regex-tester.js";
import { t } from "../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">${de ? "Entwicklung" : "Developer"}</p><h1>Regex Tester</h1><p class="page-summary">${de ? "Reguläre Ausdrücke lokal gegen Testtext prüfen." : "Test regular expressions locally against sample text."}</p></div></header><section class="card tool-workspace"><label>${de ? "Muster" : "Pattern"}<input id="regex-pattern" autocomplete="off" spellcheck="false"></label><label>${de ? "Flags" : "Flags"}<input id="regex-flags" value="g" autocomplete="off" spellcheck="false"></label><label>${de ? "Testtext" : "Test text"}<textarea id="regex-value" rows="8"></textarea></label><button id="regex-run" type="button">${de ? "Prüfen" : "Test"}</button><p id="regex-error" role="alert"></p><output id="regex-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const error = container.querySelector("#regex-error"); const output = container.querySelector("#regex-output"); const run = () => { try { output.textContent = testRegex(container.querySelector("#regex-pattern").value, container.querySelector("#regex-value").value, container.querySelector("#regex-flags").value).join("\n") || "—"; error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }; container.querySelector("#regex-run").addEventListener("click", run); }
