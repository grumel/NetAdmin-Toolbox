import { decodeUrl, encodeUrl } from "../url-encoder.js";
import { t } from "../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">${de ? "Entwicklung" : "Developer"}</p><h1>URL Encoder</h1><p class="page-summary">${de ? "URL-Komponenten lokal kodieren und dekodieren." : "Encode and decode URL components locally."}</p></div></header><section class="card tool-workspace"><label>${de ? "Eingabe" : "Input"}<textarea id="url-input" rows="8"></textarea></label><button id="url-encode" type="button">${de ? "Kodieren" : "Encode"}</button><button id="url-decode" type="button">${de ? "Dekodieren" : "Decode"}</button><p id="url-error" role="alert"></p><output id="url-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const input = container.querySelector("#url-input"); const output = container.querySelector("#url-output"); const error = container.querySelector("#url-error"); const run = (fn) => { try { output.textContent = fn(input.value); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }; container.querySelector("#url-encode").addEventListener("click", () => run(encodeUrl)); container.querySelector("#url-decode").addEventListener("click", () => run(decodeUrl)); }
