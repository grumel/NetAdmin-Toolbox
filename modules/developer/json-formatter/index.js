import { formatJson } from "../json-formatter.js";
import { t } from "../../assets/js/i18n.js";
export function render() { const de = t("network") === "Netzwerk"; return `<section><header class="page-header"><div><p class="eyebrow">${de ? "Entwicklung" : "Developer"}</p><h1>${de ? "JSON-Formatierer" : "JSON Formatter"}</h1><p class="page-summary">${de ? "JSON lokal prüfen und lesbar formatieren." : "Validate and pretty-print JSON locally."}</p></div></header><section class="card tool-workspace"><label>${de ? "JSON-Eingabe" : "JSON input"}<textarea id="json-input" rows="10" spellcheck="false"></textarea></label><label>${de ? "Einrückung" : "Indentation"}<select id="json-spaces"><option value="2">2</option><option value="4">4</option><option value="0">0</option></select></label><button id="json-format" type="button">${de ? "Formatieren" : "Format"}</button><p id="json-error" role="alert"></p><output id="json-output" aria-live="polite"></output></section></section>`; }
export function initialize(container) { const input = container.querySelector("#json-input"); const output = container.querySelector("#json-output"); const error = container.querySelector("#json-error"); const run = () => { try { output.textContent = formatJson(input.value, Number(container.querySelector("#json-spaces").value)); error.textContent = ""; } catch (exception) { output.textContent = ""; error.textContent = exception.message; } }; container.querySelector("#json-format").addEventListener("click", run); input.addEventListener("input", () => { if (input.value.trim()) run(); }); }
